<Window x:Class="Celsius.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Celsius — Akıllı Sıcaklık İzleyici"
        Icon="/Resources/app.ico"
        Width="1200" Height="800" MinWidth="960" MinHeight="640"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BgBrush}"
        Closing="Window_Closing">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Üst gezinme çubuğu: marka + sekmeler (soldan sağa) -->
        <Border Grid.Row="0" Background="{StaticResource CardAltBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <DockPanel>
                <Border DockPanel.Dock="Left" Height="56" Padding="18,0"
                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="❄" FontSize="18" Foreground="{StaticResource AccentBrush}"
                                   VerticalAlignment="Center"/>
                        <TextBlock Text="Celsius" Foreground="{StaticResource TextBrush}"
                                   FontSize="18" FontWeight="Bold" Margin="8,0,10,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Akıllı sıcaklık izleyici" Foreground="{StaticResource MutedBrush}"
                                   FontSize="11" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <ListBox ItemsSource="{Binding NavItems}"
                         SelectedIndex="{Binding SelectedNavIndex}"
                         Background="Transparent" BorderThickness="0" Padding="8,0"
                         VerticalAlignment="Center"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Foreground" Value="{StaticResource MutedBrush}"/>
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="bd" Background="Transparent" CornerRadius="8"
                                                Padding="14,8" Margin="0,2,4,2">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding Icon}" Foreground="{StaticResource AccentBrush}"
                                                           FontSize="13" Width="20" VerticalAlignment="Center"/>
                                                <TextBlock Text="{Binding Label}" FontSize="13" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="bd" Property="Background" Value="#26303C"/>
                                                <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="bd" Property="Background" Value="#1E2631"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </DockPanel>
        </Border>

        <!-- İçerik -->
        <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}" Focusable="False"/>

        <!-- Alt not -->
        <Border Grid.Row="2" Padding="16,8" Background="{StaticResource CardAltBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
            <TextBlock Text="Sensör verisi yerel okunur · Geçmiş saklanmaz · Yönetici modunda önerilir"
                       Foreground="{StaticResource MutedBrush}" FontSize="10" TextWrapping="Wrap"
                       TextAlignment="Center"/>
        </Border>
    </Grid>
</Window>
